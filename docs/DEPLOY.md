# Deploy

How to take a built 1bit.cashier from the public repo to a real shop installation.

> **Status: stub.** Will be filled in alongside `v0.2.0` (real hardware install). See the canonical spec at [`specs/2026-05-13-1bit-cashier-design.md`](specs/2026-05-13-1bit-cashier-design.md) for env-var conventions (e.g., `CASHIER_CATALOG_PATH`), systemd unit layout, audit-log backup target, and the bench-vs-real switch.
